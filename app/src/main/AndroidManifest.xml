<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maquinadegauss">

    <!-- Sección de permisos (si es necesario añadir más permisos, incluirlos aquí) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaquinadeGauss">

        <!-- Actividad principal de la aplicación -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SimulacionActivity"></activity>

        <!-- Declaración de la actividad Historial -->
        <activity
            android:name=".HistorialActivity"
            android:exported="true">
        </activity>

        <!-- Declaración del servicio de simulación -->
        <service
            android:name=".SimulacionService"
            android:exported="false" />

    </application>

</manifest>

